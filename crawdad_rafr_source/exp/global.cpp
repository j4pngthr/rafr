#include"global.hpp"

const int num_methods = 4, n = 7;
int num_cache = 2, num_off = 1;
// const vector<int> vi({ DEGREE, BETWEENNESS, RAFR });
const vector<int> vi({ DEGREE, BETWEENNESS, CONNECTIVITY, RAFR });
// const vector<string> vs({ "DEGREE", "BETWEENNESS", "RAFR" });
const vector<string> vs({ "DEGREE", "BETWEENNESS", "CONNECTIVITY", "RAFR" });
